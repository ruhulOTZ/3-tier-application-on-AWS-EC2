# Copy this file to .env on the Application-tier EC2 and fill in real values.
# Do NOT commit the real .env file to git.

PORT=3001

# Private IP of the Data-tier EC2 (recommended) or its public IP
DB_HOST=10.0.8.229
DB_PORT=5432
DB_NAME=notesdb
DB_USER=notesuser
DB_PASSWORD=change_me_strong_password
